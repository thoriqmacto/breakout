# Dependencies for the scripts in this directory (get_stocks.py).
#
# Install them into a virtualenv rather than the system interpreter, then point
# PYTHON_BIN at that virtualenv's python:
#
#     sudo apt install python3-venv        # Debian/Ubuntu only
#     python3 -m venv .venv
#     .venv/bin/pip install -r resources/python/requirements.txt
#
# On a Debian or Ubuntu server the system interpreter cannot be used: PEP 668
# marks it externally-managed and pip refuses to install into it.
pandas>=2.0
yfinance>=0.2
